# ═══════════════════════════════════════════════════════════════
#  ИНСТРУКЦИЯ ПО ВСТРАИВАНИЮ NODE.JS В EXE
# ═══════════════════════════════════════════════════════════════

## 🎯 ЦЕЛЬ
Создать полностью автономный EXE файл, который НЕ требует установки Node.js на целевой машине.

## 📋 ВАРИАНТЫ СБОРКИ

### ✅ ВАРИАНТ 1: Портативный Node.js (РЕКОМЕНДУЕТСЯ)

Этот метод самый надежный и создает полностью автономный EXE.

#### Шаг 1: Скачать портативный Node.js

1. Перейдите на https://nodejs.org/en/download
2. Скачайте **Windows Binary (.zip)** для вашей архитектуры:
   - `node-vXX.XX.XX-win-x64.zip` для 64-bit
   - `node-vXX.XX.XX-win-x86.zip` для 32-bit

#### Шаг 2: Подготовить папку

1. Создайте папку `nodejs_portable` в корне проекта
2. Распакуйте содержимое ZIP файла в эту папку

Структура должна быть такой:
```
node_bridge_project/
├── nodejs_portable/
│   ├── node.exe          ← Главный файл
│   ├── node.lib
│   ├── *.dll             ← Все DLL файлы
│   └── ...
├── src/
├── server.js             ← Ваш сервер
└── ...
```

#### Шаг 3: Положите server.js в корень проекта

Скопируйте ваш файл `server.js` в корень `node_bridge_project/`

#### Шаг 4: Собрать с портативной версией

```cmd
pyinstaller --clean NodeBridge_portable.spec
```

✅ **Результат**: EXE с встроенным Node.js и server.js!

---

### ⚙️ ВАРИАНТ 2: Установленный Node.js

Использует системный Node.js, который уже установлен на вашей машине.

#### Требования:
- Node.js должен быть установлен
- `node.exe` должен быть в PATH

#### Сборка:

1. Положите `server.js` в корень проекта
2. Запустите:
```cmd
build.bat
```
или
```cmd
pyinstaller --clean NodeBridge.spec
```

⚠️ **ВНИМАНИЕ**: Этот метод может не работать, если:
- Node.js установлен не в стандартную директорию
- Не все файлы Node.js будут включены
- На целевой машине могут отсутствовать системные библиотеки

---

### 🔧 ВАРИАНТ 3: Без встроенного Node.js

Если нужен минимальный размер EXE.

1. В `NodeBridge.spec` закомментируйте секции с Node.js:
```python
# node_binaries = []
# node_datas = []
binaries=[]
datas=[('server.js', '.')]  # Только server.js
```

2. Соберите EXE
3. **На целевой машине** потребуется установить Node.js

---

## 📦 ПРОВЕРКА СБОРКИ

После сборки проверьте:

### 1. Размер EXE
```cmd
dir dist\NodeBridge.exe
```
- **С Node.js**: ~30-50 MB
- **Без Node.js**: ~10-15 MB

### 2. Тест запуска
```cmd
dist\NodeBridge.exe
```

Смотрите в консоли приложения:
- ✅ "Запуск Node.js сервера"
- ✅ "SERVER_READY"
- ❌ "Node.js не найден" - значит не встроен

### 3. Проверка во временной папке

При запуске EXE распаковывает файлы в:
```
C:\Users\[Username]\AppData\Local\Temp\_MEI[xxxxx]\
```

Там должны быть:
- `nodejs\node.exe`
- `server.js`

---

## 🐛 РЕШЕНИЕ ПРОБЛЕМ

### "Node.js не найден" при запуске EXE

**Причина**: Node.js не был встроен в EXE

**Решение**:
1. Используйте **ВАРИАНТ 1** (портативный Node.js)
2. Убедитесь что папка `nodejs_portable` существует
3. Пересоберите с `NodeBridge_portable.spec`

### Большой размер EXE (>100 MB)

**Причина**: Включены лишние файлы из node_modules

**Решение**:
1. Используйте только `node.exe` без node_modules
2. В `.spec` файле оставьте только node.exe:
```python
if os.path.exists(node_exe):
    node_binaries.append((node_exe, 'nodejs'))
# НЕ добавляйте node_modules
```

### "Не удалось запустить server.js"

**Причина**: server.js не был включен в сборку

**Решение**:
1. Убедитесь что `server.js` находится в корне проекта
2. Проверьте в `.spec` файле:
```python
if os.path.exists('server.js'):
    node_datas.append(('server.js', '.'))
```

### DLL ошибки при запуске

**Причина**: Отсутствуют системные библиотеки

**Решение**:
1. Включите все `.dll` из папки Node.js
2. Добавьте в `.spec`:
```python
for file in os.listdir(PORTABLE_NODE_DIR):
    if file.endswith('.dll'):
        dll_path = os.path.join(PORTABLE_NODE_DIR, file)
        node_binaries.append((dll_path, 'nodejs'))
```

---

## ✅ ФИНАЛЬНЫЙ ЧЕКЛИСТ

Для полностью автономного EXE:

- [ ] Скачан портативный Node.js (ZIP)
- [ ] Распакован в `nodejs_portable/`
- [ ] `server.js` находится в корне проекта
- [ ] Использован `NodeBridge_portable.spec`
- [ ] Сборка выполнена успешно
- [ ] EXE запускается без ошибок
- [ ] Node.js сервер стартует (видно в логах)
- [ ] Chrome открывается корректно
- [ ] На целевой машине **НЕ ТРЕБУЕТСЯ** Node.js ✅

---

## 📊 СРАВНЕНИЕ МЕТОДОВ

| Метод | Размер EXE | Автономность | Сложность |
|-------|------------|--------------|-----------|
| Портативный Node.js | 30-50 MB | ✅ Полная | ⭐ Легко |
| Системный Node.js | 10-15 MB | ⚠️ Частичная | ⭐⭐ Средне |
| Без Node.js | 5-10 MB | ❌ Нет | ⭐ Легко |

**Рекомендация**: Используйте **портативный Node.js** для максимальной совместимости!

---

## 🔗 ПОЛЕЗНЫЕ ССЫЛКИ

- Node.js Downloads: https://nodejs.org/en/download
- PyInstaller Docs: https://pyinstaller.org/en/stable/
- Troubleshooting: См. README.md
